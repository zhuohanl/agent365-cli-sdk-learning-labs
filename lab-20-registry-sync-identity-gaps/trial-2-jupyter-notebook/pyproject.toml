[project]
name = "registry-sync-identity-notebook"
version = "0.1.0"
description = "A bounded Agent 365 Registry Sync identity notebook"
requires-python = ">=3.12"
dependencies = [
    "ipykernel>=6.29,<8",
    "msal>=1.31,<2",
    "requests>=2.32,<3",
]

[project.optional-dependencies]
test = [
    "nbclient>=0.10,<1",
    "nbformat>=5.10,<6",
]

[tool.uv]
package = false
